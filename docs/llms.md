!!! info "Why this page exists"
    This is a single, high-density reference designed to make an AI coding
    assistant (Claude, ChatGPT, Copilot, …) an **expert Ikarus user** in one read —
    every feature, convention and sharp edge, condensed. The same text **ships
    inside the installed package**, so any chat can load it without this site:

    ```bash
    python -c "import ikarus; print(ikarus.ai_guide())"
    ```

    It is the single source of truth (the page below is generated from
    `ikarus/AI_GUIDE.md`), so it never drifts from the code. Point your assistant
    here, or paste the command output into a fresh conversation.

--8<-- "ikarus/AI_GUIDE.md"
